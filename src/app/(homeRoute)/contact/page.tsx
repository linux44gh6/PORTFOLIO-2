import ContactPage from "@/components/Contact/ContactPage";

const page = () => {
    return (
        <div className="min-h-screen">
            <ContactPage/>
        </div>
    );
};

export default page;