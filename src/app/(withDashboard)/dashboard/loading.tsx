import Spinner from '@/components/ui/Sppiner';
import React from 'react';

const LoadingPage = () => {
    return (
        <div>
            <Spinner/>
        </div>
    );
};

export default LoadingPage;